# 模型 (Model)

第一步是创建一个项目并添加不同的 Burn 依赖项。首先使用 Cargo 创建一个新项目：

```bash
cargo new guide
```

如[之前所述](../getting-started.html#creating-a-burn-application)，这将初始化您的 `guide` 项目目录，包含一个 `Cargo.toml` 和一个 `src/main.rs` 文件。

在 `Cargo.toml` 文件中，添加带有 `train`、`vision` 和 `wgpu` 特性的 `burn` 依赖项。由于我们禁用了默认特性，我们还需要为 wgpu 启用 `std`、`tui`（用于仪表板）和 `fusion`。然后运行 `cargo build` 来构建项目并导入所有依赖项。

```toml
[package]
name = "guide"
version = "0.1.0"
edition = "2024"

[dependencies]
# 禁用卷积的自动调优默认设置
burn = { version = "~0.19", features = ["std", "tui", "train", "vision", "wgpu", "fusion"], default-features = false }
# burn = { version = "~0.19", features = ["train", "vision", "wgpu"] }
```

我们的目标是创建一个用于图像分类的基本卷积神经网络。我们将通过使用两个卷积层后跟两个线性层、一些池化和 ReLU 激活函数来保持模型简单。我们还将使用 dropout 来改善训练性能。

让我们开始在新文件 `src/model.rs` 中定义我们的模型结构体。

```rust
use burn::{
    nn::{
        conv::{Conv2d, Conv2dConfig},
        pool::{AdaptiveAvgPool2d, AdaptiveAvgPool2dConfig},
        Dropout, DropoutConfig, Linear, LinearConfig, Relu,
    },
    prelude::*,
};

#[derive(Module, Debug)]
pub struct Model<B: Backend> {
    conv1: Conv2d<B>,
    conv2: Conv2d<B>,
    pool: AdaptiveAvgPool2d,
    dropout: Dropout,
    linear1: Linear<B>,
    linear2: Linear<B>,
    activation: Relu,
}
```

这个代码示例中有两个主要的事情正在发生。

1. 您可以使用结构体顶部的 `#[derive(Module)]` 属性创建深度学习模块。这将生成必要的代码，使结构体实现 `Module` trait。这个 trait 将使您的模块既可训练又可（反）序列化，同时添加相关功能。像 Rust 中经常使用的其他属性一样，如 `Clone`、`PartialEq` 或 `Debug`，结构体中的每个字段也必须实现 `Module` trait。

<details>
<summary><strong>🦀 Trait</strong></summary>
<p>Traits 是 Rust 语言中一个强大而灵活的特性。它们提供了一种为特定类型定义共享行为的方式，这些行为可以与其他类型共享。</p>

<p>类型的行为包括在该类型上调用的方法。由于所有 `Module` 都应该实现相同的功能，因此它被定义为一个 trait。在特定类型上实现 trait 通常需要用户为其类型实现 trait 的定义行为，但如上所述，这里不是这种情况，因为有 `derive` 属性。查看下面的<a href="#derive-attribute">解释器</a>来了解原因。</p>

<p>有关 traits 的更多详细信息，请查看 Rust Book 中的<a href="https://doc.rust-lang.org/book/ch10-02-traits.html">相关章节</a>。</p>
</details><br>

<details id="derive-attribute">
<summary><strong>🦀 Derive Macro</strong></summary>
<p>`derive` 属性允许通过生成代码来轻松实现 traits，这些代码将在用 `derive` 语法注释的类型上实现带有自己默认实现的 trait。</p>

<p>这是通过 Rust 的一个称为<a href="https://doc.rust-lang.org/reference/procedural-macros.html">过程宏</a>的特性来完成的，它允许我们...
</details>

```rust
use burn::{
    nn::{
        conv::{Conv2d, Conv2dConfig},
        pool::{AdaptiveAvgPool2d, AdaptiveAvgPool2dConfig},
        Dropout, DropoutConfig, Linear, LinearConfig, Relu,
    },
    prelude::*,
};

#[derive(Module, Debug)]
pub struct Model<B: Backend> {
    conv1: Conv2d<B>,
    conv2: Conv2d<B>,
    pool: AdaptiveAvgPool2d,
    dropout: Dropout,
    linear1: Linear<B>,
    linear2: Linear<B>,
    activation: Relu,
}

#[derive(Config, Debug)]
pub struct ModelConfig {
    num_classes: usize,
    hidden_size: usize,
    #[config(default = "0.5")]
    dropout: f64,
}

impl ModelConfig {
    /// 返回初始化的模型。
    pub fn init<B: Backend>(&self, device: &B::Device) -> Model<B> {
        Model {
            conv1: Conv2dConfig::new([1, 8], [3, 3]).init(device),
            conv2: Conv2dConfig::new([8, 16], [3, 3]).init(device),
            pool: AdaptiveAvgPool2dConfig::new([8, 8]).init(),
            activation: Relu::new(),
            linear1: LinearConfig::new(16 * 8 * 8, self.hidden_size).init(device),
            linear2: LinearConfig::new(self.hidden_size, self.num_classes).init(device),
            dropout: DropoutConfig::new(self.dropout).init(),
        }
    }
}
```

乍一看，您可以通过打印模型实例来查看模型配置：

```rust
#![recursion_limit = "256"]
mod model;

use crate::model::ModelConfig;
use burn::backend::Wgpu;

fn main() {
    type MyBackend = Wgpu<f32, i32>;

    let device = Default::default();
    let model = ModelConfig::new(10, 512).init::<MyBackend>(&device);

    println!("{model}");
}
```

输出：

```rust
Model {
  conv1: Conv2d {ch_in: 1, ch_out: 8, stride: [1, 1], kernel_size: [3, 3], dilation: [1, 1], groups: 1, padding: Valid, params: 80}
  conv2: Conv2d {ch_in: 8, ch_out: 16, stride: [1, 1], kernel_size: [3, 3], dilation: [1, 1], groups: 1, padding: Valid, params: 1168}
  pool: AdaptiveAvgPool2d {output_size: [8, 8]}
  dropout: Dropout {prob: 0.5}
  linear1: Linear {d_input: 1024, d_output: 512, bias: true, params: 524800}
  linear2: Linear {d_input: 512, d_output: 10, bias: true, params: 5130}
  activation: Relu
  params: 531178
}
```

<details>
<summary><strong>🦀 References</strong></summary>
<p>在前面的示例中，`init()` 方法签名使用 `&` 来表示参数类型是引用：`&self`，对当前接收者（`ModelConfig`）的引用，和 `device: &B::Device`，对后端设备的引用。</p>

```rust
pub fn init<B: Backend>(&self, device: &B::Device) -> Model<B> {
    Model {
        // ...
    }
}
```

<p>Rust 中的引用允许我们指向一个资源来访问其数据而不拥有它。所有权的概念是 Rust 的核心，值得<a href="https://doc.rust-lang.org/book/ch04-00-understanding-ownership.html">阅读了解</a>。</p>

<p>在像 C 这样的语言中，内存管理是明确的，由程序员负责，这意味着很容易犯错误。在像 Java 或 Python 这样的语言中，内存管理通过垃圾收集器的帮助是自动的。这非常安全和直接，但也会产生运行时成本。</p>

<p>在 Rust 中，内存管理相当独特。除了实现<a href="https://doc.rust-lang.org/std/marker/trait.Copy.html"><code>Copy</code></a>的简单类型（例如，<a href="https://doc.rust-lang.org/rust-by-example/primitives.html">基本类型</a>如整数、浮点数、布尔值和 `char`），每个值都由某个称为<em>所有者</em>的变量<em>拥有</em>。所有权可以从一个变量转移到另一个变量，有时一个值可以被<em>借用</em>。一旦<em>所有者</em>变量超出作用域，值就会被<em>丢弃</em>，这意味着它可以安全地释放其分配的任何内存。</p>

<p>因为方法不拥有 `self` 和 `device` 变量，所以值引用指向的内容在引用停止使用时（即方法的作用域）不会被丢弃。</p>

<p>有关引用和借用的更多信息，请务必阅读 Rust Book 中的<a href="https://doc.rust-lang.org/book/ch04-02-references-and-borrowing.html">相应章节</a>。</p>
</details><br>

当创建自定义神经网络模块时，通常最好在模型结构体旁边创建一个配置。这允许您为网络定义默认值，这要归功于 `Config` 属性。这个属性的好处是它使配置可序列化，使您能够轻松保存模型超参数，增强您的实验过程。请注意，将为您的配置自动生成一个构造函数，它将接受没有默认值的输入值参数：`let config = ModelConfig::new(num_classes, hidden_size);`。默认值可以通过类似构建器的方法轻松覆盖：（例如 `config.with_dropout(0.2);`）

第一个实现块与初始化方法相关。正如我们所看到的，所有字段都是使用相应神经网络底层模块的配置来设置的。在这种特定情况下，我们选择在第一层中将张量通道从 1 扩展到 8，然后在第二层中从 8 扩展到 16，在所有维度上使用 3 的内核大小。我们还使用自适应平均池化模块将图像的维度减少到 8x8 矩阵，我们将在前向传递中将其展平为 1024（16 * 8 * 8）的结果张量。

现在让我们看看前向传递是如何定义的。

```rust
use burn::{
    nn::{
        conv::{Conv2d, Conv2dConfig},
        pool::{AdaptiveAvgPool2d, AdaptiveAvgPool2dConfig},
        Dropout, DropoutConfig, Linear, LinearConfig, Relu,
    },
    prelude::*,
};

#[derive(Module, Debug)]
pub struct Model<B: Backend> {
    conv1: Conv2d<B>,
    conv2: Conv2d<B>,
    pool: AdaptiveAvgPool2d,
    dropout: Dropout,
    linear1: Linear<B>,
    linear2: Linear<B>,
    activation: Relu,
}

#[derive(Config, Debug)]
pub struct ModelConfig {
    num_classes: usize,
    hidden_size: usize,
    #[config(default = "0.5")]
    dropout: f64,
}

impl ModelConfig {
    /// 返回初始化的模型。
    pub fn init<B: Backend>(&self, device: &B::Device) -> Model<B> {
        Model {
            conv1: Conv2dConfig::new([1, 8], [3, 3]).init(device),
            conv2: Conv2dConfig::new([8, 16], [3, 3]).init(device),
            pool: AdaptiveAvgPool2dConfig::new([8, 8]).init(),
            activation: Relu::new(),
            linear1: LinearConfig::new(16 * 8 * 8, self.hidden_size).init(device),
            linear2: LinearConfig::new(self.hidden_size, self.num_classes).init(device),
            dropout: DropoutConfig::new(self.dropout).init(),
        }
    }
}

impl<B: Backend> Model<B> {
    /// # 形状
    ///   - 图像 [batch_size, height, width]
    ///   - 输出 [batch_size, num_classes]
    pub fn forward(&self, images: Tensor<B, 3>) -> Tensor<B, 2> {
        let [batch_size, height, width] = images.dims();

        // 在第二维度创建通道。
        let x = images.reshape([batch_size, 1, height, width]);

        let x = self.conv1.forward(x); // [batch_size, 8, _, _]
        let x = self.dropout.forward(x);
        let x = self.conv2.forward(x); // [batch_size, 16, _, _]
        let x = self.dropout.forward(x);
        let x = self.activation.forward(x);

        let x = self.pool.forward(x); // [batch_size, 16, 8, 8]
        let x = x.reshape([batch_size, 16 * 8 * 8]);
        let x = self.linear1.forward(x);
        let x = self.dropout.forward(x);
        let x = self.activation.forward(x);

        self.linear2.forward(x) // [batch_size, num_classes]
    }
}
```

对于以前的 PyTorch 用户，这可能感觉非常直观，因为每个模块都直接使用急切 API 融入到代码中。请注意，前向方法没有强加任何抽象。您可以自由地定义多个具有您喜欢的名称的前向函数。大多数用 Burn 构建的神经网络模块都使用 `forward` 命名法，这只是因为它是该领域的标准。

与神经网络模块类似，作为参数给出的<a href="../building-blocks/tensor.html"><code>Tensor</code></a> 结构体也将 Backend trait 作为泛型参数，以及其维度。即使在这个特定示例中没有使用，也可以添加张量类型作为第三个泛型参数。例如，不同数据类型（float、int、bool）的 3 维张量将定义如下：

```rust
Tensor<B, 3> // 浮点张量（默认）
Tensor<B, 3, Float> // 浮点张量（显式）
Tensor<B, 3, Int> // 整数张量
Tensor<B, 3, Bool> // 布尔张量
```

请注意，特定的元素类型，如 `f16`、`f32` 等，稍后将使用后端定义。

---

*本文档翻译自 Burn 官方文档：https://burn.dev/books/burn/basic-workflow/model.html*
